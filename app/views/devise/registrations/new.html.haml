.text-center{:style => "padding:50px 0"}
  .logo TECH::SHARE 新規登録
  .login-form-1
    #register-form.text-left
      = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
        .login-form-main-message
        .main-login-form
          .login-group
            .form-group
              = f.label :nickname,class: "sr-only"
              = f.text_field :nickname, autofocus: true, autocomplete: "nickname", id: "reg_username", class: "form-control", placeholder: "nickname"
            .form-group
              = f.label :email, class: "sr-only", for: "reg_email"
              = f.email_field :email, autofocus: true, autocomplete: "email", id: "reg_email", class: "form-control", placeholder: "email"
            .form-group
              = f.label :password, class: "sr-only", for: "reg_password"
              = f.password_field :password, autocomplete: "new-password", id: "reg_password", class: "form-control", placeholder: "password",type: "password"
            .form-group
              = f.label :password_confirmation, class: "sr-only", for: "reg_password_confirm"
              = f.password_field :password_confirmation, autocomplete: "new-password", id: "reg_password_confirm", class: "form-control",placeholder: "confirm password"
              - if @minimum_password_length
                %em
                  (#{@minimum_password_length} 文字以上)
            = button_tag type: 'submit',class: "login-button bg-warning" do
              %i.fa.fa-chevron-right
        .etc-login-form
          %p
            すでにアカウントを持っている?
            %br
            = render "devise/shared/links"
