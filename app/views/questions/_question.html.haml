.alert.alert-warning
  %h5.alert-heading
    = link_to question_path(question),class: "alert-link" do
      = question.title
  %hr
    .d-flex.justify-content-between
      .name
        質問者：
        =question.user.nickname
      .date
        投稿日時：
        =question.created_at.strftime("%Y年%-m月%-d日 %-H時%-M分")